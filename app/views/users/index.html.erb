<% provide(:title, 'All users') %>
<h1>All users</h1>

<% if signed_in? %>
  You are now signed in!
     <% else %>
     <li><%= link_to "Sign in", signin_path %></li>
<% end %>

<%= will_paginate %>

<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate %>